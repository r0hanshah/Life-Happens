import TaskModel from "../models/TaskModel"
import UserModel from "../models/UserModel"

const MAIL_URL = 'http://127.0.0.1:4000'

// Send mail notification
export const schedule_start_email_notification = async (user: UserModel, task: TaskModel) => {
    
}

export const schedule_end_email_notification = async (user: UserModel, task: TaskModel) => {

}

//const schedule_email = async ()

// Delete mail notification

// Reschedule mail notification